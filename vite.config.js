import { defineConfig } from 'vite'

export default defineConfig({
  // ... existing config ...
  build: {
    target: 'esnext',
    outDir: 'dist',
    assetsDir: 'assets',
    rollupOptions: {
      output: {
        manualChunks: undefined
      }
    }
  }
}) 